# Railway Frontend Configuration
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "if [ -n \"$PORT\" ]; then serve -s dist -l $PORT; else serve -s dist; fi"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
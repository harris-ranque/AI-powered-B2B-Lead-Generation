# Railway Frontend Configuration
[build]
builder = "NIXPACKS"
buildCommand = "node convex-mock.cjs && npm run build"

[deploy]  
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
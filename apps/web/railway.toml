# Railway Frontend Configuration
[build]
builder = "NIXPACKS"
buildCommand = "node convex-mock.cjs && pnpm build"

[deploy]  
startCommand = "pnpm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
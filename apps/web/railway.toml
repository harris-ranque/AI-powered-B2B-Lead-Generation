# Railway Frontend Configuration - Build from monorepo root
[build]
builder = "DOCKERFILE"
# Build from monorepo root to access convex-backend for codegen
context = "../.."
dockerfile = "apps/web/Dockerfile"
# Exit on test failures to prevent deployment of broken code
buildCommand = "echo 'Starting build with integrated testing...'"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
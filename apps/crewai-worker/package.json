{
  "name": "@genni/crewai-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8080",
    "start": "python -m hypercorn app.main:app --bind \"[::]:$PORT\"",
    "install": "pip install -r requirements.txt"
  },
  "engines": {
    "python": ">=3.11"
  }
}